import styles from './main.module.css';
import Header from "../header/header";

const Main = () => {
  return (
    <main className={styles.container}>
      <Header />
    </main>
  );
};

export default Main;
